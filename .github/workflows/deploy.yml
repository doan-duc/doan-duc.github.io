# Build the static export and deploy it to GitHub Pages.
#
# Pages must be configured once by hand: Settings → Pages → Source: "GitHub Actions".
# After that, every push to main ships exactly the `out/` directory that the
# test suites exercise — the repository root no longer carries a copy of the
# export, so the deployed site can never drift behind the source again.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run typecheck

      - run: npm run build

      # Fast content checks against the freshly built export — the same files
      # that are about to deploy.
      - run: npm run test:static

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
