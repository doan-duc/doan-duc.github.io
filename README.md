# Doan Sinh Duc Portfolio

Static portfolio website for GitHub Pages.

## Run locally

```powershell
python -m http.server 8123
```

Open `http://127.0.0.1:8123`.

## Deploy

Push the `profilo` folder to the repository or branch you want to publish, then point GitHub Pages at the folder root. Update the custom domain later through the GitHub Pages settings.
